# 截图与提交操作指南

本文档从当前实验目录开始说明操作。当前目录应为：

```bash
cd /mnt/e/migratedfromc/desktop/homework/2xia/计算机设计实验/lab-6
```

建议所有终端输出日志和截图都放在：

```text
交付的实验报告/日志
交付的实验报告/截图素材
```

如果目录不存在，先执行：

```bash
mkdir -p "交付的实验报告/日志" "交付的实验报告/截图素材"
```

## 一、测试 1：单周期 CPU 的 30 条指令测试

测试 1 使用教师给出的 `Test_30_Instr.dat`。为了既能得到寄存器输出，又能用 GTKWave 看波形，这里使用已经添加好的 `sc_final_wave_tb.v`。执行：

```bash
cd /mnt/e/migratedfromc/desktop/homework/2xia/计算机设计实验/lab-6/source-sc

iverilog -I . -s sc_final_wave_tb -o sc_final_wave.out \
  alu.v ctrl.v dm.v EXT.v im.v NPC.v PC.v sccomp.v SCCPU.v RF.v \
  sc_final_wave_tb.v

vvp sc_final_wave.out \
  +IMEM=../CODExp/testcode/task/Test_30_Instr.dat \
  +CYCLES=120 \
  +VCD=sc_final_test30.vcd \
  | tee ../交付的实验报告/日志/测试1-单周期30条指令.log
```

截图时保留终端中以下内容：

```text
vvp sc_final_wave.out +IMEM=../CODExp/testcode/task/Test_30_Instr.dat +CYCLES=120 +VCD=sc_final_test30.vcd
[REG] x...
[MEM] m...
[VCD] sc_final_test30.vcd
```

建议截取若干关键寄存器，比如 `x1`、`x2`、`x3`、`x5`、`x10` 附近的输出。Windows 下按 `Win + Shift + S`，框选终端区域，保存为：

```text
交付的实验报告/截图素材/测试1-单周期30条指令.png
```

生成波形后，可以打开 GTKWave：

```bash
gtkwave sc_final_test30.vcd
```

运行时如果看到类似下面的 warning，可以忽略：

```text
$readmemh(...): Not enough words in the file for the requested range [0:127].
```

原因是 `Test_30_Instr.dat` 没有填满 128 个指令存储单元，仿真仍会正常运行并生成 VCD。

如果 `gtkwave` 窗口没有弹出，通常是 WSL 图形界面没有连通。可以在 Windows 文件管理器中打开下面的 VCD 文件：

```text
E:\migratedfromc\desktop\homework\2xia\计算机设计实验\lab-6\source-sc\sc_final_test30.vcd
```

GTKWave 左侧层级展开：

```text
sc_final_wave_tb
  dut
    PC
    instr
    MemWrite
    dm_addr
    dm_din
    dm_dout
    U_SCCPU
```

建议加入波形窗口的信号：

```text
sc_final_wave_tb.clk
sc_final_wave_tb.rstn
sc_final_wave_tb.dut.PC
sc_final_wave_tb.dut.instr
sc_final_wave_tb.dut.MemWrite
sc_final_wave_tb.dut.dm_addr
sc_final_wave_tb.dut.dm_din
sc_final_wave_tb.dut.dm_dout
sc_final_wave_tb.dut.U_SCCPU.RegWrite
sc_final_wave_tb.dut.U_SCCPU.ALUSrc
sc_final_wave_tb.dut.U_SCCPU.ALUOp
sc_final_wave_tb.dut.U_SCCPU.NPCOp
sc_final_wave_tb.dut.U_SCCPU.RD1
sc_final_wave_tb.dut.U_SCCPU.RD2
sc_final_wave_tb.dut.U_SCCPU.B
sc_final_wave_tb.dut.U_SCCPU.aluout
sc_final_wave_tb.dut.U_SCCPU.WD
```

GTKWave 截图建议保存为：

```text
交付的实验报告/截图素材/测试1-单周期30条指令-波形.png
```

## 二、测试 2：流水线 CPU 的 30 条指令测试

测试 2 同样使用 `Test_30_Instr.dat`，但换成流水线 CPU 和 `pl_final_wave_tb.v`。执行：

```bash
cd /mnt/e/migratedfromc/desktop/homework/2xia/计算机设计实验/lab-6/source-pipeline

iverilog -I . -s pl_final_wave_tb -o pl_final_wave.out \
  PipelineCPU.v pipe_decode.v pipe_alu.v pipe_branch.v hazard_unit.v \
  dm.v im.v plcomp.v RF.v \
  pl_final_wave_tb.v

vvp pl_final_wave.out \
  +IMEM=../CODExp/testcode/task/Test_30_Instr.dat \
  +CYCLES=200 \
  +VCD=pl_final_test30.vcd \
  | tee ../交付的实验报告/日志/测试2-流水线30条指令.log
```

截图时保留终端中以下内容：

```text
vvp pl_final_wave.out +IMEM=../CODExp/testcode/task/Test_30_Instr.dat +CYCLES=200 +VCD=pl_final_test30.vcd
[REG] x...
[MEM] m...
[VCD] pl_final_test30.vcd
```

保存截图为：

```text
交付的实验报告/截图素材/测试2-流水线30条指令.png
```

打开流水线波形：

```bash
gtkwave pl_final_test30.vcd
```

运行时如果看到类似下面的 warning，可以忽略：

```text
$readmemh(...): Not enough words in the file for the requested range [0:127].
```

原因同样是测试程序没有填满整个指令 RAM。

如果 WSL 不能弹出 GTKWave 窗口，可以在 Windows 文件管理器中打开：

```text
E:\migratedfromc\desktop\homework\2xia\计算机设计实验\lab-6\source-pipeline\pl_final_test30.vcd
```

GTKWave 左侧层级展开：

```text
pl_final_wave_tb
  dut
    PC
    instr
    MemWrite
    U_PLCPU
      ifid_valid
      ifid_pc
      ifid_inst
      idex_valid
      idex_pc
      idex_rs1
      idex_rs2
      idex_rd
      exmem_valid
      exmem_alu_result
      memwb_valid
```

建议加入波形窗口的信号：

```text
pl_final_wave_tb.clk
pl_final_wave_tb.rstn
pl_final_wave_tb.dut.PC
pl_final_wave_tb.dut.instr
pl_final_wave_tb.dut.MemWrite
pl_final_wave_tb.dut.U_PLCPU.ifid_valid
pl_final_wave_tb.dut.U_PLCPU.ifid_pc
pl_final_wave_tb.dut.U_PLCPU.ifid_inst
pl_final_wave_tb.dut.U_PLCPU.idex_valid
pl_final_wave_tb.dut.U_PLCPU.idex_pc
pl_final_wave_tb.dut.U_PLCPU.idex_rs1
pl_final_wave_tb.dut.U_PLCPU.idex_rs2
pl_final_wave_tb.dut.U_PLCPU.idex_rd
pl_final_wave_tb.dut.U_PLCPU.exmem_valid
pl_final_wave_tb.dut.U_PLCPU.exmem_alu_result
pl_final_wave_tb.dut.U_PLCPU.memwb_valid
pl_final_wave_tb.dut.U_PLCPU.memwb_alu_result
pl_final_wave_tb.dut.U_PLCPU.forward_a
pl_final_wave_tb.dut.U_PLCPU.forward_b
pl_final_wave_tb.dut.U_PLCPU.pc_write
pl_final_wave_tb.dut.U_PLCPU.ifid_write
pl_final_wave_tb.dut.U_PLCPU.idex_flush
pl_final_wave_tb.dut.U_PLCPU.ifid_flush
pl_final_wave_tb.dut.U_PLCPU.ex_take_branch
```

流水线波形截图建议保存为：

```text
交付的实验报告/截图素材/测试2-流水线30条指令-波形.png
```

如果只想一条命令生成 VCD 并打开 GTKWave，也可以执行：

```bash
cd /mnt/e/migratedfromc/desktop/homework/2xia/计算机设计实验/lab-6/source-sc
make wave30

cd /mnt/e/migratedfromc/desktop/homework/2xia/计算机设计实验/lab-6/source-pipeline
make wave30
```

## 三、测试 3：单周期 CPU 学号排序

测试 3 使用单周期 CPU 的排序 testbench，程序会检查原始学号和排序结果。执行：

```bash
cd /mnt/e/migratedfromc/desktop/homework/2xia/计算机设计实验/lab-6/source-sc

make run | tee ../交付的实验报告/日志/测试3-单周期学号排序.log
```

看到下面结果表示通过：

```text
[RESULT] original_sid=54873530
[RESULT] sorted_sid=03345578
[PASS] lab-6 sid sorting simulation passed.
```

截图时保留上述三行和 `make run` 命令，保存为：

```text
交付的实验报告/截图素材/测试3-单周期学号排序.png
```

## 四、测试 4：流水线 CPU 学号排序

测试 4 使用流水线 CPU 的排序 testbench。执行：

```bash
cd /mnt/e/migratedfromc/desktop/homework/2xia/计算机设计实验/lab-6/source-pipeline

make run | tee ../交付的实验报告/日志/测试4-流水线学号排序.log
```

看到下面结果表示通过：

```text
[RESULT] original_sid=54873530
[RESULT] sorted_sid=03345578
[PASS] lab-6 pipeline sid sorting simulation passed.
```

截图时保留上述三行和 `make run` 命令，保存为：

```text
交付的实验报告/截图素材/测试4-流水线学号排序.png
```

## 五、最终扩展指令总测试

这不是 `needs.txt` 中单独列出的四个截图之一，但建议也跑一次，作为报告和答辩时的总证据。执行：

```bash
cd /mnt/e/migratedfromc/desktop/homework/2xia/计算机设计实验/lab-6

python3 final-验收文档/run_final_tests.py \
  | tee 交付的实验报告/日志/final-扩展指令总测试.log
```

通过时末尾应为：

```text
Summary: 16/16 passed
```

可选截图保存为：

```text
交付的实验报告/截图素材/final-扩展指令总测试.png
```

## 六、开发板照片

开发板加分项需要真实照片或视频截图。推荐现场展示流程如下：

```text
1. 给开发板上电并复位。
2. 数码管先显示原始学号 54873530。
3. 按下开始按钮触发排序。
4. 数码管显示排序结果 03345578。
5. 拍照时让开发板、数码管和按键区域同时入镜。
```

照片文件建议命名为：

```text
交付的实验报告/截图素材/测试5-开发板流水线排序.jpg
```

如果只能截图视频，也保存为同名 `.png` 或 `.jpg`。

## 七、代码截图怎么截

报告里需要解释一条运算指令和一条跳转指令。现在报告按流水线 CPU 来解释 `add` 和 `jalr`，所以代码截图也应以 `source-pipeline` 下的流水线代码为主。截图建议按下面的编号保存，编号和报告中的占位名称一致。

### 1. 运算指令 add 的流水线代码截图

截图 A1：`add` 的译码和 ID/EX 保存。

打开文件：

```bash
code -g source-pipeline/pipe_decode.v:57
code -g source-pipeline/PipelineCPU.v:287
```

截图范围和重点：

```text
source-pipeline/pipe_decode.v 第 57--72 行：
  R-type 译码，{funct7, funct3}=0000000_000 时设置 alu_op = PIPE_ALU_ADD。

source-pipeline/PipelineCPU.v 第 318--322 行：
  IF/ID 保存 ifid_valid、ifid_pc、ifid_inst。

source-pipeline/PipelineCPU.v 第 287--305 行：
  ID/EX 保存 rf_rd1、rf_rd2、id_rs1、id_rs2、id_rd、id_alu_op、id_reg_write 等。
```

保存截图为：

```text
交付的实验报告/截图素材/A1-add-decode-idex.png
```

截图 A2：`add` 的执行、转发输入和写回路径。

打开文件：

```bash
code -g source-pipeline/PipelineCPU.v:136
code -g source-pipeline/PipelineCPU.v:250
code -g source-pipeline/pipe_alu.v:10
```

截图范围和重点：

```text
source-pipeline/PipelineCPU.v 第 136--149 行：
  forward_a/forward_b 选择 ALU 输入，说明 RAW 冒险时可以选择 EX/MEM 或 MEM/WB 的值。

source-pipeline/PipelineCPU.v 第 151--156 行：
  pipe_alu U_ALU 实例化，说明 EX 阶段把 alu_in_a、alu_in_b 和 idex_alu_op 交给 ALU。

source-pipeline/pipe_alu.v 第 10--14 行：
  PIPE_ALU_ADD 执行 result = a + b。

source-pipeline/PipelineCPU.v 第 250--264 行：
  EX/MEM 到 MEM/WB 的结果传递。
```

保存截图为：

```text
交付的实验报告/截图素材/A2-add-ex-wb.png
```

### 2. 跳转指令 jalr 的流水线代码截图

截图 B1：`jalr` 的译码和 ID/EX 控制信号保存。

打开文件：

```bash
code -g source-pipeline/pipe_decode.v:115
code -g source-pipeline/PipelineCPU.v:291
```

截图范围和重点：

```text
source-pipeline/pipe_decode.v 第 115--120 行：
  opcode=1100111 时识别 jalr，设置 imm、jalr、alu_src、reg_write。

source-pipeline/PipelineCPU.v 第 291--305 行：
  ID/EX 保存 id_imm、id_rs1、id_rd、id_reg_write、id_alu_src、id_jalr。
```

保存截图为：

```text
交付的实验报告/截图素材/B1-jalr-decode-idex.png
```

截图 B2：`jalr` 的跳转判断、跳转目标和流水线冲刷。

打开文件：

```bash
code -g source-pipeline/pipe_branch.v:29
code -g source-pipeline/PipelineCPU.v:308
code -g source-pipeline/hazard_unit.v:30
```

截图范围和重点：

```text
source-pipeline/pipe_branch.v 第 29--31 行：
  take_branch 判断 jalr 必定跳转，branch_target 计算 (src_a + imm) & 32'hffff_fffe。

source-pipeline/PipelineCPU.v 第 308--317 行：
  ex_take_branch 成立时 PC 更新为 ex_branch_target，并清空 IF/ID。

source-pipeline/hazard_unit.v 第 30--33 行：
  ex_take_branch 触发 idex_flush 和 ifid_flush。
```

保存截图为：

```text
交付的实验报告/截图素材/B2-jalr-branch-flush.png
```

### 3. 流水线区别和冒险处理代码截图

截图 C1：流水线段寄存器。

打开文件：

```bash
code -g source-pipeline/PipelineCPU.v:20
```

截图范围和重点：

```text
source-pipeline/PipelineCPU.v 第 20--62 行：
  IF/ID、ID/EX、EX/MEM、MEM/WB 四组段寄存器。
```

保存截图为：

```text
交付的实验报告/截图素材/C1-pipeline-registers.png
```

截图 C2：RAW 数据转发。

打开文件：

```bash
code -g source-pipeline/hazard_unit.v:35
```

截图范围和重点：

```text
source-pipeline/hazard_unit.v 第 35--43 行：
  forward_a 和 forward_b，展示 EX/MEM 转发和 MEM/WB 转发。
```

保存截图为：

```text
交付的实验报告/截图素材/C2-forwarding.png
```

截图 C3：load-use 暂停和 bubble。

打开文件：

```bash
code -g source-pipeline/hazard_unit.v:25
```

截图范围和重点：

```text
source-pipeline/hazard_unit.v 第 25--33 行：
  load_use_hazard 检测，pc_write/ifid_write 暂停，idex_flush 插入 bubble。
```

保存截图为：

```text
交付的实验报告/截图素材/C3-load-use-stall.png
```

如果没有安装 VS Code 命令行 `code`，可以直接在文件管理器或编辑器中打开这些文件并跳到对应行号。Windows 下截图仍然使用 `Win + Shift + S`。

## 八、生成 diff 统计截图

老师要求 diff 文件修改截图，只需要展示修改了哪些文件、每个文件改了多少行，不需要展开完整 diff。

当前目录不是完整 git 仓库，推荐用 `git diff --no-index --stat` 和老师给的 demo 源码做对比，生成统计文本。执行：

```bash
cd /mnt/e/migratedfromc/desktop/homework/2xia/计算机设计实验/lab-6

{
  echo "===== 单周期 CPU 与 demo 对比 ====="
  git diff --no-index --stat CODExp/demo/sccpu_sim/source source-sc || true
  echo
  echo "===== 流水线 CPU 与 demo 对比 ====="
  git diff --no-index --stat CODExp/demo/plcpu_sim/source source-pipeline || true
  echo
  echo "===== 多周期 CPU 文件统计 ====="
  wc -l source-mc/MCCPU.v source-mc/*.v
} | tee 交付的实验报告/日志/diff-stat.txt
```

然后查看前 120 行：

```bash
sed -n '1,120p' 交付的实验报告/日志/diff-stat.txt
```

截图保存为：

```text
交付的实验报告/截图素材/diff统计.png
```

如果你已经把代码上传到 GitHub，也可以在 GitHub 网页打开 commit 或 compare 页面，只截图顶部的文件列表和增删行数统计。

## 九、上传 GitHub

如果当前代码还没有上传 GitHub，可以按下面流程建立仓库并推送。先进入实验根目录：

```bash
cd /mnt/e/migratedfromc/desktop/homework/2xia/计算机设计实验/lab-6
```

初始化 git 仓库并提交：

```bash
git init
git add source-sc source-mc source-pipeline nexys-a7 final-验收文档 验收指南.md 验收速查.md 交付的实验报告
git commit -m "complete lab6 cpu design and report"
```

在 GitHub 网页新建一个空仓库，例如：

```text
lab6-cpu-design
```

然后按 GitHub 页面给出的命令推送。常见命令如下，把 `你的用户名` 替换成自己的 GitHub 用户名：

```bash
git branch -M main
git remote add origin https://github.com/你的用户名/lab6-cpu-design.git
git push -u origin main
```

推送成功后，把仓库链接填入报告：

```text
https://github.com/你的用户名/lab6-cpu-design
```

报告中的占位位置在：

```bash
code -g "交付的实验报告/report/main.tex:435"
```

把：

```text
https://github.com/username/repository
```

替换成你的真实仓库链接。

## 十、把截图插入 LaTeX 报告

当前报告里已经有文字占位框。要替换为真实图片，需要修改：

```text
交付的实验报告/report/main.tex
```

建议先把图片复制到：

```text
交付的实验报告/report/images/
```

执行：

```bash
mkdir -p "交付的实验报告/report/images"
cp "交付的实验报告/截图素材/测试1-单周期30条指令.png" "交付的实验报告/report/images/"
cp "交付的实验报告/截图素材/测试2-流水线30条指令.png" "交付的实验报告/report/images/"
cp "交付的实验报告/截图素材/测试3-单周期学号排序.png" "交付的实验报告/report/images/"
cp "交付的实验报告/截图素材/测试4-流水线学号排序.png" "交付的实验报告/report/images/"
cp "交付的实验报告/截图素材/diff统计.png" "交付的实验报告/report/images/"
```

如果有开发板照片，也复制进去：

```bash
cp "交付的实验报告/截图素材/测试5-开发板流水线排序.jpg" "交付的实验报告/report/images/"
```

在 `main.tex` 中找到类似下面的占位行：

```tex
\placeholder{测试1截图占位：单周期 CPU 30 条指令测试。请替换为仿真窗口或终端截图，并选取若干关键寄存器值。}{4cm}
```

替换成：

```tex
\begin{figure}[H]
\centering
\includegraphics[width=0.92\textwidth]{images/测试1-单周期30条指令.png}
\caption{测试1：单周期 CPU 30 条指令测试结果}
\end{figure}
```

其他截图按同样方式替换文件名和 caption。替换后重新编译：

```bash
cd /mnt/e/migratedfromc/desktop/homework/2xia/计算机设计实验/lab-6/交付的实验报告/report

xelatex -interaction=nonstopmode main.tex
xelatex -interaction=nonstopmode main.tex
```

生成的 PDF 为：

```text
交付的实验报告/report/main.pdf
```

如果要同步到交付目录：

```bash
cp -f 交付的实验报告/report/main.pdf 交付的实验报告/实验报告.pdf
```

注意：上面这条 `cp` 命令要在实验根目录执行。如果你当前已经在 `交付的实验报告/report` 目录下，应执行：

```bash
cp -f main.pdf ../实验报告.pdf
```

## 十一、最终提交前检查

提交前在实验根目录执行：

```bash
cd /mnt/e/migratedfromc/desktop/homework/2xia/计算机设计实验/lab-6

python3 final-验收文档/run_final_tests.py
make -C source-sc run
make -C source-pipeline run
ls -lh 交付的实验报告/实验报告.pdf
```

确认看到：

```text
Summary: 16/16 passed
[PASS] lab-6 sid sorting simulation passed.
[PASS] lab-6 pipeline sid sorting simulation passed.
```

然后检查报告中这些内容是否已替换：

```text
1. 单位、姓名、学号。
2. GitHub 链接。
3. 测试1、测试2、测试3、测试4截图。
4. 开发板照片或说明。
5. diff 统计截图。
```
